#include <stdio.h>
int main() {
    unsigned int i = 10;
    
    printf("This loop will never end!\n");

    /*
    while (i >= 0) {  // Always true for unsigned!
        printf("%u ", i);
        i--;
    }
    */
    
    return 0;
}
